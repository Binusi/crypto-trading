# Expo only inlines env vars prefixed EXPO_PUBLIC_ at build time.
# Use your dev machine's LAN IP, not localhost — Expo Go runs on the phone.
# Find it on macOS Wi-Fi with: ipconfig getifaddr en0
EXPO_PUBLIC_API_URL=http://192.168.1.10:8000
